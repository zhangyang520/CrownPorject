<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/ali_header_text_padding" >

        <ProgressBar
            android:id="@+id/default_footer_progressbar"
            android:layout_width="@dimen/ali_header_progressbar"
            android:layout_height="@dimen/ali_header_progressbar"
            android:layout_centerInParent="true"
            android:indeterminateDuration="800"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/default_footer_title"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="@color/springview_text_color"
            android:textSize="@dimen/default_text_size"
            android:text="@string/default_footer_title" />
    </RelativeLayout>

</LinearLayout>