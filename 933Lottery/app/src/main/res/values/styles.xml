<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- pop窗口弹出缩放动画-->
    <style name="popupWindowScaleAnimation">
        <item name="android:windowEnterAnimation">@anim/pop_input_view_scale_in</item>
        <item name="android:windowExitAnimation">@anim/pop_input_view_scale_out</item>
    </style>

    <style name="popupWindowSheetAnimation">
        <item name="android:windowEnterAnimation">@anim/pop_input_view_in</item>
        <item name="android:windowExitAnimation">@anim/pop_input_view_out</item>
    </style>

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="loading_dialog" parent="android:Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <style name="xloading_padding_style">
        <item name="android:paddingTop">@dimen/XFrame_spacing</item>
        <item name="android:paddingBottom">@dimen/XFrame_spacing</item>
        <item name="android:paddingLeft">@dimen/XFrame_spacing_large</item>
        <item name="android:paddingRight">@dimen/XFrame_spacing_large</item>
    </style>
</resources>
